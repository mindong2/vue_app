export default {
  SET_NEWS(state, news) {
    state.news = news;
  },

  SET_ASKS(state, ask) {
    state.ask = ask;
  },

  SET_JOBS(state, jobs) {
    state.jobs = jobs;
  },

  SET_USER(state, name) {
    state.user = name;
  },

  SET_ITEM(state, item) {
    state.item = item;
  },
};
